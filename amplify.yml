version: 1
backend:
  phases:
    build:
      commands:
        - npm ci --cache .npm --prefer-offline
        - npm install @parcel/watcher-linux-x64-glibc@2.5.1 --no-save --no-package-lock --legacy-peer-deps
        - |
          SCHEMA1="node_modules/@aws-amplify/deployed-backend-client/node_modules/@aws-amplify/backend-output-schemas/lib/platform/stack_metadata_schemas.js"
          SCHEMA2="node_modules/@aws-amplify/backend-cli/node_modules/@aws-amplify/backend-output-schemas/lib/platform/stack_metadata_schemas.js"
          for f in "$SCHEMA1" "$SCHEMA2"; do
            [ -f "$f" ] && sed -i 's/z\.record(exports\.backendOutputEntryStackMetadataSchema)/z.record(z.string(), exports.backendOutputEntryStackMetadataSchema)/g' "$f" && echo "Patched $f"
          done
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/admin/browser
    files:
      - '**/*'
  cache:
    paths:
      - '.npm/**/*'
